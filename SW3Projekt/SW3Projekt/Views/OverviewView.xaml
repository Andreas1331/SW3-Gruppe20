<UserControl x:Class="SW3Projekt.Views.OverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SW3Projekt.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Button x:Name="BtnSaldoOverview" FontSize="20" Grid.Row="0" Grid.Column="0" Content="Se Saldo oversigt" BorderThickness="0" Background="{Binding BtnSaldoBackground}" Foreground="{DynamicResource UltraWhiteBrush}"></Button>

        <Button x:Name="BtnYearlyOverview" FontSize="20" Grid.Row="0" Grid.Column="1" Content="Se årlig oversigt" BorderThickness="0" Background="{Binding BtnYearlyBackground}" Foreground="{DynamicResource UltraWhiteBrush}"></Button>

        <ContentControl x:Name="ActiveItem" IsTabStop="False" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"></ContentControl>

    </Grid>
</UserControl>
